# URL of the BudgetForge backend (leave empty to use Next.js proxy / same origin)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8011

# Dashboard password protection (leave empty to disable auth in dev mode)
DASHBOARD_PASSWORD=
SESSION_SECRET=
